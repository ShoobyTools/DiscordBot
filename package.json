{
  "name": "discordbot",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "del-cli build && npx tsc",
    "start": "npm run build && node ./build/bot.js",
    "dev": "npm run build && node ./build/deploy-commands-local.js && node ./build/bot.js",
    "deploy": "npm run build && node ./build/deploy-commands-global.js && node ./build/bot.js",
    "shopify": "npm run build && node ./build/modules/shopify.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.0",
    "cheerio": "^1.0.0-rc.12",
    "del-cli": "^5.0.0",
    "discord.js": "^14.7.1",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "typescript": "^4.8.2"
  }
}
